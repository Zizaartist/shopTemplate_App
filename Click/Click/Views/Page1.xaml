<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Click.Views.Page1">
    <ContentPage.Content>
        <StackLayout>
            <Slider BindingContext="{x:Reference Star}"
                    Minimum="0"
                    Maximum="65"
                    Value="{Binding WidthRequest}"/>
            <Grid Style="{StaticResource StarGridBackground}"
                  HorizontalOptions="Center">
                <Grid Style="{StaticResource StarGrid}"
                      x:Name="Star"/>
                <Image Style="{StaticResource StarRating}"/>
            </Grid>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>